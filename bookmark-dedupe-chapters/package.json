{
  "name": "bookmark-dedupe-chapters",
  "version": "1.0.0",
  "description": "Tries to remove all bookmarks/favorites that have an increment chapter",
  "author": "Sebastian Grüb",
  "license": "MIT",
  "scripts": {
    "tsc": "tsc",
    "build": "tsc && cp ./src/resources/* ./build",
    "watch": "npm-watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.127",
    "npm-watch": "^0.7.0",
    "typescript": "^4.1.3"
  },
  "watch": {
    "build": {
      "patterns": [
        "src"
      ],
      "extensions": "ts,json,html,png",
      "quiet": true,
      "delay": 1000
    }
  }
}
